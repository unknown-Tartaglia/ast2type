function foo(x) {
    return x;
}
function bar(x) {
    return x;
}
let a = foo(5);

let onLoadResult = (success) => {
    const logInfo =  `用户头像加载成功: ${success}}` 
 }

onLoadResult(true);

class demo {
    method1(arg) {
        return arg;
    }

    run() {
        const result = this.method1(10);
    }
}